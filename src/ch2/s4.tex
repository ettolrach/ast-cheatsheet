\wax{3: A function is determined by its effect on elements} For all sets $X$ and $Y$ and functions $f, g \colon X \to Y$, if $f(x) = g(x)$ for all $x \in X$ then $f = g$.
\wl{2.4.1} A set is terminal iff it has exactly one elem.
